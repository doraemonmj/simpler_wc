name: CI

permissions:
  contents: read
  pull-requests: read

on:
  pull_request:
    branches: [main, ci/streamline-ut]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  packaging-macos:
    uses: ./.github/workflows/_packaging.yml
    with:
      runs_on: '["macos-latest"]'
      setup_variant: github
      python_version: "3.10"
